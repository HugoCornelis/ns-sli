#!/usr/bin/perl -w
#!/usr/bin/perl -d:ptkdb
#
#
#

use strict;


# $Format: "my $package_name = \"${package}\";"$
my $package_name = "ns-sli";

# $Format: "my $package_version = \"${label}\";"$
my $package_version = "userdocs-6";

my $core_directory = '../';

my $config
    = {
       core_directory => $core_directory,
       description => 'Configure the tester when run from this directory',
       enviroment_settings => {
			       NEUROSPACES_NMC_MODELS => {
							  value => $core_directory . '/src',
							 },
			      },
       outputs_dir => './html',
       package => {
		   name => $package_name,
		   version => $package_version,
		  },
       tests_directory => './specifications',
      };


return $config;

